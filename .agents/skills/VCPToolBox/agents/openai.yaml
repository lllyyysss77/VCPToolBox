interface:
  display_name: "VCPToolBox"
  short_description: "Repo-specific patterns and workflows for VCPToolBox"
  default_prompt: "Use the VCPToolBox repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true